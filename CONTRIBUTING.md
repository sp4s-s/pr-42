You are more than welcome to try this and build onto it ...

## Code Standards
- Don't break schema; minimal changes.
- Use small var names (e.g., `x`, `y`, `df`).
- No breaking changes without PR discussion.
- PyTorch/PyTorch Lightning; type hints.